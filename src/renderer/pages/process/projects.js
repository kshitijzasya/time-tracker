import React from 'react';
import {

} from 'react-bootstrap';
import { BasicLayout } from '../../components/layouts/basic';

const Projects = _ => { console.log('inside projects')
    return (
        <>
        <p>Projects</p>
        </>
    )
}

export default BasicLayout(Projects);